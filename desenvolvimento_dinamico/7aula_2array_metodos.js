/*
Métodos básicos de Array:
push() / pop() / shift()

push: "Empurrar" elementos ao array, irá adicionar na próximma posição no array
pop: "Retira" elementos do array, expele elementos da última posição para a primeira
shift: "Remove a esquerda" elementos do array, expele elementos da primeira posição do array
*/

// Métodos básicos:

let frutas = ["maçã", "banana", "laranja", "uva"];

console.log("Array inicial: " + frutas);
console.log("Comprimento do array: " + frutas.length);